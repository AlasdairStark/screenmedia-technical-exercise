@model Screenmedia.ToDo.Web.Models.ToDoNotes.ToDoNotesViewModel;

@{
    ViewData["Title"] = "List All ToDo Notes";
}

<div>
    @foreach (var toDoNote in @Model.ToDoNotes)
    {
        await Html.RenderPartialAsync("_ToDoNote", toDoNote);
    }
</div>